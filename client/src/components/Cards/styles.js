import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  loadingPaper: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "300px",
  },
});
