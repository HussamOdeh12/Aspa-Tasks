import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  ul: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    padding: "5px",
  },
}));
