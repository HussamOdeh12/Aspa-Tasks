import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  paginate: {
    marginTop: "8px",
  },

  ul: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: "10px",
  },

  item: {
    margin: "5px",
  },
}));
